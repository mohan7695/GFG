class Solution {
	public static int findMean(int[] arr) {
		// code here
		int n = arr.length;
		int sum = 0;
		for (int k : arr) {
			sum += k;
			
		}
		int d = (int)Math.floor(sum/n);
		
		return d;
	}
};
